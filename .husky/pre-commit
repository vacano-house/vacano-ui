pnpm lint-staged
pnpm typecheck

# Update version badge in README.md from package.json
VERSION=$(node -p "require('./package.json').version")
sed -i '' "s/version-[0-9]*\.[0-9]*\.[0-9]*-blue/version-${VERSION}-blue/g" README.md
git add README.md
