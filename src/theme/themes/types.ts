export type Theme = {
  chip: {
    color: {
      default: string
      neutral: string
      success: string
      warning: string
      error: string
    }
    backgroundColor: {
      default: string
      neutral: string
      success: string
      warning: string
      error: string
    }
  }
}
